# DESIGN — BrainExporter 视觉世界

> 方向契约(2026-08-18 会话锁定,用户三选确认)
> 视觉世界:沉浸星球主页 × 极简工具页的双域反差

## 世界观

BrainExporter 是"一部手机里的开源脑电工作台"。视觉世界建立在**双域反差**上:主页是深空仪式(沉浸、氛围、情绪峰值),工具页是实验室工作台(极简、克制、数据即视觉)。用户在这两种节奏之间往返,反差本身就是品牌记忆点。

- **Experience 域(星球主页)**:星尘、光晕、3D 自转、氛围动画。这里引诱,这里惊艳。
- **Operate 域(监测/模块/音频/我的)**:精密排版、低噪音、高信息密度。这里工作,这里可信。
- 两个域共享一套语义色(青=良好/连接、琥珀=过渡、红=错误、紫=模块),共享统一圆角与间距节奏,共享等宽数字。

## 色板(深色唯一,user-pinned)

- 基础:Ink `#07101C`(底)、Panel `#101F33`(面)、PanelSoft `#0D192A`(浮层)
- 主色:Cyan `#38D9C5`(连接/良好/主线)、Blue `#6A8DFF`(次要/信息)
- 强调:Amber `#FFBD5B`(警告/过渡)、Danger `#FF6B7A`(错误)
- 特征:FeaturePurple `#A991FF`(模块特征)、ChannelColors 8 色(tabular 数据)
- 文本:TextPrimary `#EDF7FF`、TextMuted `#8EA5BB`
- 仅深色,不引入浅色/Dynamic Color(用户明确承诺)。

## 类型

- 沿用 Material 类型缩放;所有数值数据用等宽数字(tabular numerals),量值(Hz/kΩ/µV/%)横向对齐。
- 轴标签 ≥11sp 且随系统字体缩放(已改)。
- 标题层级:页头 headlineMedium 粗体 → 卡标题 SemiBold → 信息 labelSmall(已收敛)。

## 组件语言

- 卡片:统一圆角 16dp、双层表面(Ink 底 / Panel 面/浮层)、无阴影边框微光;近期内不新增嵌套卡片。
- 芯片/标签:统一胶囊形 + 语义色 10% 淡底。
- 图标:Material Icons(已替换 Unicode),装饰性图标不占语义。
- 按钮:主操作 Cyan 填充,危险操作 Danger 填充,次级 Outlined + Text。

## 动效原则

- 一个主打时刻:星球自转+大气脉冲(已有);其余动效克制:页面切换淡入、按钮涟漪、图表曲线更新。
- 所有无限动画仅在相关状态激活时运行(加载/播放),空闲状态零动画。
- 尊重 Reduce Motion:播放/旋转类动画在系统减弱动画设置下退化为静态。

## 验收基准

- 主页第一屏:3 秒内传达"这是一颗活着的星球",光晕/星尘/自转肉眼可辨。
- 监测页:一眼定位主任务(连接/采集),1 屏内不超负荷,信息密度比现状降 30%。
- 构建:assembleDebug 通过;TalkBack 可朗读全部交互元素;字号 1.3x 无截断。